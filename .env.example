# Gmail address used to SEND the contact form emails (needs a Gmail App Password, not your normal password)
EMAIL_USER=your-gmail@gmail.com
EMAIL_PASS=your-16-char-app-password

# Optional: where contact form messages should be delivered. Defaults to EMAIL_USER if unset.
CONTACT_TO_EMAIL=your-gmail@gmail.com
